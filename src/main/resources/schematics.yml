# Configure all the schematics available in /island create
schematics:
  default:
    display-name: '&#a6b2fc&lDefault Island'
    icon: OAK_LOG
    lore:
      - ' &f| &7This is the default option'
      - ' &f| &7for creating an island.'